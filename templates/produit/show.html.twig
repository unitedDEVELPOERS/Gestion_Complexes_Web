{% extends 'Admin/index.html.twig' %}

{% block body %}
    <h1>Produit</h1>

    <table class="table" border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ produit.reference }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ produit.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ produit.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ produit.price }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ produit.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}">back to list</a>

    &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; <a class="btn-danger" href="{{ path('produit_edit', {'id': produit.id}) }}">  Edit </a>


    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}







