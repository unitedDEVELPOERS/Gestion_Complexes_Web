{% extends 'Arbitre/index.html.twig' %}

{% block body %}



    <div class="form-row align-items-end">
        <div class="col">

        </div>
        <div class="col">
            <div class="form-group">
                <form></form>
                <input type="text" name="recherche">
                <button type="submit" class="btnbtn-success">Rechercher</button>
                </form>
            </div>
        </div>
    </div>




            <h3><i class="fa fa-angle-right"></i> LISTE DES Penalites</h3>


            <div class="row mt">
                <div class="col-md-12">
                    <div class="content-panel">
                        <table class="table table-striped table-advance table-hover">
                            <h4>
                                    <div class="float-right" style="margin-right: 90px">
                                        <a href="{{ path('penalite') }}" class="btn btn-primary"> Nouveau Penalite</a>
                                    </div><br>

                                </h4>
                            <hr>
                            <thead>
                            <tr>

                                <th>Designation</th>
                                <th>NbrePointsRetires</th>



                            </tr>
                            </thead>
                            <tbody>
                            <tr {% for p in penalites %}  >

                                <td>{{ p.designation }}</td>
                                <td>{{ p.nbrePointsRetires }}</td>


                                <td style="display: inline-block">
                                    <button class="btn btn-success btn-xs"><i class="fa fa-check"></i></a>
                                            <form method="post" action="{{ path ('UpdatePenalite', {'id':p.id}) }}"  onsubmit="return confirm('êtes vous sûr de modifier cette Penalite ? ')">
                                                <input type="hidden" name="_method" value="UPDATE">

                                                <input type="hidden" name="_token" value="{{ csrf_token('Update' ~p.id) }}">
                                                <button class="btn btn-primary btn-xs"></button><i class="fa fa-pencil"></i></a>
                                            </form>
                                                    <form method="post" action="{{ path ('dp', {id: p.id}) }}"  onsubmit="return confirm('êtes vous sûr de supprimer cette Penalite ? ')">
                                                        <input type="hidden" name="_method" value="DELETE">
                                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ p.id) }}">
                                                        <button class="btn btn-danger btn-xs"><p class="fa fa-trash-o "></button>

                                                    </form>





                              <!--  <td>
                                    <button class="btn btn-success btn-xs"><i class="fa fa-check"></i></button>
                                    <button class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i></button>
                                    <button class="btn btn-danger btn-xs"><i class="fa fa-trash-o "></i></button>
                                </td>-->
                            </tr {% endfor %} >

                            </tbody>
                        </table>
                    </div>
                    <!-- /content-panel -->
                </div>
                <!-- /col-md-12 -->
            </div>
            <!-- /row -->
        </section>
    </section>
    <!-- /MAIN CONTENT -->
    <!--main content end-->

{% endblock %}